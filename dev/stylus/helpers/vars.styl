$os = "ProximaNova"
$lf = "Lifehack"

$gblue = #e7edf9
$shadow = #f5f7fc
$blue = #0066b3
$red = #f1151d
$dred = #cc1219
$orange = #fe670e

$print = 'print'

$Mobile = '(max-width:340px)'
$Mobile370 = '(max-width:370px)'

$PortraitMin = '(max-width:390px)'
$PortraitMedium = '(max-width:500px)'
$PortraitMediumFrom = '(min-width:501px)'
$PortraitMax = '(max-width:670px)'
$PortraitMaxFrom = '(min-width:671px)'

$LandscapeMin = '(max-width:767px)'
$LandscapeMinFrom = '(min-width:768px)'
$LandscapeMedium = '(max-width:950px)'
$LandscapeMediumMin = '(min-width:951px)'
$LandscapeMax = '(max-width:1000px)'
$LandscapeMaxFrom = '(min-width:1001px)'

$desktopTablet = '(max-width:1050px)'

$dekstop1142 = '(max-width:1142px)'
$desktopMin = '(max-width:1170px)'
$desktopMedium = '(max-width:1270px)'
$desktopMediumFrom = '(min-width:1271px)'
$desktopMax = '(max-width:1400px)'

imgCentering()
  position absolute
  left 50%
  top 50%
  transform translate(-50%, -50%) scale(1)
  transition 1s transform
  will-change transform
  
imgCenteringMax90()
  position absolute
  left 50%
  top 50%
  max-width 90%
  max-height 90%
  transform translate(-50%, -50%) scale(1)
  transition 1s transform
  will-change transform
  
imgCenteringMax100()
  position absolute
  left 50%
  top 50%
  max-width 100%
  max-height 100%
  transform translate(-50%, -50%) scale(1)
  transition 1s transform
  will-change transform

imgCenteringMin()
  position absolute
  left 50%
  top 50%
  min-height 100%
  min-width 100%
  transform translate(-50%, -50%) scale(1)
  will-change transform
  
imgCenteringZoom()
  transform translate(-50%, -50%) scale(1.02)
  
size($w, $h = $w)
  width $w
  height $h
  
ib($align = top)
  display inline-block
  vertical-align $align
  
fontify($fs, $fw, $lh)
  font-size $fs
  font-weight $fw
  line-height $lh
  
oh()
  overflow hidden

w($val)
  width $val

h($val)
  height $val
  
t($val)
  top $val
  
r($val)
  right $val
  
l($val)
  left $val
  
b($val)
  bottom $val

bgc($val)
  background-color $val
  
dn()
  display none
  
db()
  display block
  
df($at = flex-start, $jc = flex-start)
  display flex
  align-items $at
  justify-content $jc
  
dfv($at = flex-start, $jc = flex-start)
  display flex
  flex-direction column
  align-items $at
  justify-content $jc
  
full()
  t 0
  r 0
  b 0
  l 0
  
pos($val)
  position $val
  
mr($val)
  margin-right $val

mt($val)
  margin-top $val

ml($val)
  margin-left $val
  
mb($val)
  margin-bottom $val
  
pr($val)
  padding-right $val

pt($val)
  padding-top $val

pl($val)
  padding-left $val
  
pb($val)
  padding-bottom $val
  
c($val)
  color $val